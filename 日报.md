# 大数据电商分析平台 - 项目开发日报

## 项目概述
大数据电商分析平台（DataMall）前端功能优化与Bug修复，涉及Vue3前端状态管理、组件通信、搜索功能及部署联调。

---

## 成员一：张伟（前端架构与状态管理）

### 第一天
- 分析首页分类导航点击无响应的问题，梳理Layout.vue与Home.vue之间的组件通信链路
- 排查通过`<router-view>`传递props的方案，确认Vue3中`<router-view>`无法可靠传递响应式props到子组件
- 设计基于Pinia的全局状态管理方案，创建`store/ui.js`，定义`selectedCategory`响应式状态及`setCategory`方法
- 在`main.js`中确认Pinia插件正确初始化，验证store全局可用性

### 第二天
- 重构Layout.vue，移除本地`selectedCat` ref，接入`useUiStore()`，实现分类选择与全局状态同步
- 移除Layout.vue中`provide/inject`模式及`<router-view>`的props传递逻辑，简化组件通信架构
- 重构Home.vue，移除`props.selectedCategory`，接入`useUiStore()`，通过watch监听`uiStore.selectedCategory`实现分类切换响应
- 添加关键路径debug日志（watch触发、loadProducts调用、数据加载结果），便于追踪数据流

### 第三天
- 定位搜索状态持久化Bug：搜索后URL携带`?keyword=xxx`，返回主页时keyword被清除但`searchMode`未重置
- 修复`route.query.keyword`的watch，增加`else if (searchMode.value)`分支，当keyword被清除时自动重置搜索状态并重新加载商品数据
- 移除冗余的`route.path` watch，精简状态管理逻辑
- 清理所有debug console.log语句，确保生产环境代码整洁

---

## 成员二：李娜（UI组件开发与优化）

### 第一天
- 审查首页整体UI布局，确认搜索结果页面与正常首页的显示切换逻辑
- 优化搜索模式下的页面展示：搜索时隐藏Banner、限时秒杀、热门推荐、猜你喜欢等模块，仅显示搜索结果和全部商品
- 完善搜索结果区域UI，包括搜索关键词展示、商品数量统计及"清除搜索"按钮交互
- 检查`v-if="searchMode"`条件渲染逻辑，确保各模块在搜索/非搜索模式下正确显隐

### 第二天
- 根据需求移除Layout.vue中的分类导航栏（全部、手机、电脑、耳机、平板、穿戴、游戏、摄影、配件）
- 清理Layout.vue模板中`<nav class="category-nav">`及相关子元素
- 清理Layout.vue中`categories`数组、`selectCat`函数等已废弃的JS逻辑
- 移除Layout.vue中`useUiStore`、`useRoute`、`nextTick`等不再需要的导入

### 第三天
- 清理Layout.vue中`.category-nav`、`.nav-inner`、`.nav-item`、`.nav-item.active`、`.nav-item.active::after`等已废弃的CSS样式
- 清理响应式布局中`.nav-inner`和`.nav-item`的媒体查询样式
- 验证移除分类导航后页面布局完整性，确认header、main-content、footer衔接正常
- 优化Home.vue的`clearSearch`函数，确保清除搜索后正确重置分页并重新加载商品

---

## 成员三：王强（功能测试与Bug修复）

### 第一天
- 执行分类导航功能测试，发现点击分类按钮后商品列表未过滤，确认组件间状态传递失败
- 验证provide/inject方案：Layout通过`provide('selectedCat', selectedCat)`提供状态，Home通过`inject`接收，测试发现`<router-view>`阻断了provide/inject链路
- 编写测试用例覆盖分类切换场景：同分类重复点击、跨分类切换、非首页切换分类后跳转
- 记录Bug复现步骤及预期行为，提交问题追踪

### 第二天
- 验证Pinia store方案：测试`uiStore.selectedCategory`在Layout和Home间的响应式同步
- 发现分类切换后Home组件watch未触发问题，排查Vue3 watch对Pinia ref的监听机制
- 优化watch逻辑，增加`searchMode.value = false`确保分类切换时退出搜索模式
- 执行搜索功能回归测试：搜索→查看结果→清除搜索→返回正常首页

### 第三天
- 重点测试搜索状态持久化Bug：搜索"iPhone"→点击logo返回主页→页面仍显示搜索结果
- 分析Bug根因：URL从`/?keyword=iPhone`变为`/`时，`route.query.keyword`从字符串变为undefined，但watch只处理了keyword存在的情况
- 协助验证修复方案：在keyword watch中增加else分支处理keyword清除场景
- 执行完整回归测试：搜索→返回→再搜索→切换页面→返回，确认所有路径正常

---

## 成员四：陈静（构建部署与联调）

### 第一天
- 搭建前端构建部署流程：Vite build → 输出到`frontend/dist` → 复制到`backend/src/main/resources/static`
- 确认后端Spring Boot静态资源服务配置，验证`static/`目录下`index.html`及assets正确加载
- 检查Vite构建配置（`vite.config.js`），确认Vue插件、Element Plus自动导入插件正常工作
- 验证开发环境代理配置（`/api` → `localhost:8080`），确保前后端联调通畅

### 第二天
- 执行首次完整构建部署：`npm run build`成功，生成23个文件（JS+CSS+HTML）
- 处理构建产物部署：清理旧assets目录，复制新构建产物到后端static目录
- 重启Spring Boot后端服务（`mvn spring-boot:run`），验证8080端口服务正常启动
- 排查后端启动过程中的Kafka/HBase兼容性警告，确认不影响前端功能

### 第三天
- 执行多次增量构建部署，配合前端修改快速迭代验证
- 优化部署脚本：PowerShell命令实现一键清理旧资源+复制新构建+重启后端
- 验证最终版本部署：确认搜索Bug修复、分类导航移除、搜索状态重置等功能均正常
- 整理部署文档，记录构建命令、静态资源路径、后端重启流程，便于后续维护

---

## 本次迭代成果总结

| 模块 | 改动内容 | 状态 |
|------|---------|------|
| 状态管理 | 创建Pinia uiStore，实现跨组件分类状态共享 | ✅ 已完成 |
| 搜索功能 | 修复返回主页搜索状态未清除的Bug | ✅ 已完成 |
| UI优化 | 移除分类导航栏及相关样式 | ✅ 已完成 |
| 代码质量 | 清理debug日志、废弃导入和冗余代码 | ✅ 已完成 |
| 构建部署 | 完善前端构建与后端静态资源部署流程 | ✅ 已完成 |
