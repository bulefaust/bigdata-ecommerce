# DataMall 智能电商平台 - 项目完整报告

> 最后更新：2026-05-12

---

## 一、项目概述

### 1.1 项目背景

DataMall 是一个结合 **Web 开发** 与 **大数据分析** 的智能电商平台。项目核心思路：构建一个真实的电商购物网站，通过用户行为追踪收集数据，利用大数据技术进行分析，将分析结果反馈到业务系统中，实现数据驱动的智能运营。

### 1.2 技术栈

#### 前端技术
- **Vue 3** + **Vite** 构建工具
- **Element Plus** UI 组件库
- **Pinia** 状态管理
- **Axios** HTTP 请求
- **Vue Router** 路由管理

#### 后端技术
- **Spring Boot 2.7.18**
- **MyBatis Plus** 数据持久层
- **MySQL 8.0** 关系型数据库
- **Kafka** 消息队列
- **Maven** 构建工具

#### 大数据组件
- Hadoop 3.3.4（HDFS 分布式存储）
- HBase 2.4.11（列式 NoSQL 数据库）
- Hive（数据仓库）
- Spark（实时/离线计算）
- Flink（流处理）
- Zookeeper（分布式协调）
- Kafka（消息队列）
- Flume（数据采集）

#### 部署环境
- 开发机：Windows（`192.168.100.100`）
- 大数据集群：Linux 虚拟机（`192.168.100.110`）

---

## 二、系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                         用户浏览器                           │
│   Vue3 SPA（首页/搜索/详情/购物车/订单/登录/个人中心）         │
│   Tracker SDK 自动采集用户行为数据                           │
└───────────────────────────┬────────────────────────────────┘
                            │ HTTP /api/*
                            ▼
┌──────────────────────────────────────────────────────────────┐
│              Spring Boot 后端（端口 8080）                    │
│   RESTful API · 用户认证 · 业务逻辑 · 数据持久化              │
└─────────────┬─────────────────────────────────┬──────────────┘
              │                                 │
              ▼                                 ▼
┌───────────────────────┐        ┌────────────────────────────────┐
│      MySQL 8.0         │        │      Kafka 消息队列              │
│  商品/用户/订单/评价    │        │   user-behavior Topic           │
└───────────────────────┘        └───────────────┬────────────────┘
                                                │
                                                ▼
┌──────────────────────────────────────────────────────────────┐
│                    大数据集群（192.168.100.110）               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐  │
│  │  Flume   │→│  Kafka   │→│  Flink   │→│  HBase / HDFS    │  │
│  │ 采集Agent│ │ 消息队列 │ │ 流处理   │ │  实时数据存储     │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌────────────────────────────────┐  │
│  │  Hadoop  │ │   Hive   │ │  Spark（实时/离线计算）         │  │
│  │ HDFS存储 │ │ 数据仓库  │ │  推荐/统计/用户画像             │  │
│  └──────────┘ └──────────┘ └────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

---

## 三、前端功能模块

### 3.1 页面总览

| 页面 | 路由 | 功能描述 |
|------|------|---------|
| 首页 | `/` | Banner 区（含粒子动画）、限时秒杀、热门推荐、猜你喜欢、为你推荐、商品列表 |
| 商品详情 | `/product/:id` | 商品图片轮播、价格信息、规格参数、库存进度条、用户评价、相似推荐、加购/立即购买 |
| 购物车 | `/cart` | 商品列表、数量调整、价格实时计算、结算跳转 |
| 订单确认 | `/order` | 收货地址、支付方式、订单摘要 |
| 登录 | `/login` | 用户名密码登录、Token 持久化 |
| 注册 | `/register` | 用户注册、密码验证 |
| 个人中心 | `/profile` | 用户信息展示、订单入口 |
| 数据看板 | `/dashboard` | 销售统计、用户画像、流量分析 |

### 3.2 首页特色功能

#### Banner 数据流粒子动画
- **Canvas 全屏动画**，无第三方依赖
- **数据流层**：日文片假名字符（カタカナ）从上至下流动，模拟黑客帝国 Matrix 效果
- **粒子网络层**：80-120 个微光粒子缓慢漂移，粒子间自动连线形成拓扑网络
- **配色**：青色 `#00D4FF` + 紫色 `#7C3AED`，与深蓝背景 `#0B1120` 呼应

#### 限时秒杀模块
- 后端实时查询当前时间范围内的秒杀活动
- 倒计时显示（等宽字体 + 青色发光）
- 库存进度条实时展示抢购进度
- 支持折扣标签（X折）

#### 智能推荐
- **热门推荐**：基于实时点击量统计
- **猜你喜欢**：基于随机推荐算法
- **为你推荐**：基于用户 ID 的个性化推荐（需登录）
- 支持"换一批"动态刷新

---

## 四、后端 API 接口

### 4.1 用户接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/user/login` | POST | 用户登录，返回 Token |
| `/api/user/register` | POST | 用户注册 |
| `/api/user/info` | GET | 获取当前用户信息 |

### 4.2 商品接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/products` | GET | 商品分页列表（支持分类筛选） |
| `/api/products/hot` | GET | 热门商品推荐 |
| `/api/products/random` | GET | 随机商品推荐 |
| `/api/products/personal/{userId}` | GET | 个性化推荐 |
| `/api/products/search` | GET | 关键词搜索 |
| `/api/product/{id}` | GET | 商品详情 |

### 4.3 订单接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/orders` | GET | 用户订单列表 |
| `/api/order/create` | POST | 创建订单 |
| `/api/order/{id}/pay` | POST | 订单支付 |
| `/api/order/{id}/cancel` | POST | 取消订单 |

### 4.4 秒杀接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/flash-sale/current` | GET | 当前进行中的秒杀活动 |
| `/api/flash-sale/upcoming` | GET | 即将开始的秒杀活动 |

### 4.5 数据追踪接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/track/behavior` | POST | 接收前端行为数据，写入 Kafka |

---

## 五、数据收集与追踪

### 5.1 追踪数据类型

| 行为类型 | 触发时机 | 收集方式 |
|---------|---------|---------|
| `CLICK` | 点击商品/分类/按钮 | 前端 Tracker |
| `VIEW` | 进入商品详情页 | 前端 Tracker |
| `SEARCH` | 搜索关键词提交 | 前端 Tracker |
| `ADD_CART` | 添加商品到购物车 | 前端 Tracker + 后端接口 |
| `PURCHASE` | 创建订单完成 | 后端订单系统 |
| `SCROLL` | 页面滚动深度 | 前端 Tracker |

### 5.2 追踪数据格式

```json
{
  "userId": 12345,
  "sessionId": "abc-def-ghi",
  "behaviorType": "VIEW",
  "targetId": 51,
  "targetType": "PRODUCT",
  "category": "手机",
  "pageUrl": "/product/51",
  "timestamp": "2026-05-12 20:30:00",
  "device": "PC",
  "ip": "192.168.100.100"
}
```

### 5.3 数据流

```
前端 Tracker SDK
    ↓ (异步，不阻塞用户操作)
后端 /api/track/behavior
    ↓
Kafka Topic: user-behavior
    ↓
Flink / Spark Streaming（实时消费）
    ↓
HBase（实时行为数据）
MySQL（聚合统计数据）
```

---

## 六、数据分析方案

### 6.1 离线分析（T+1）

| 分析维度 | 工具 | 输出 |
|---------|------|------|
| 销售统计（日/月/年） | Hive SQL | 销售额趋势报表 |
| 商品热度排行 | Spark TopN | 热门商品榜单 |
| 用户行为漏斗 | Spark | 转化率分析 |
| 品类销售占比 | Hive SQL | 饼图/柱状图 |
| 时段访问分析 | Hive SQL | 时段热力图 |

### 6.2 实时分析

| 分析维度 | 工具 | 应用场景 |
|---------|------|---------|
| 实时热点商品 | Spark Streaming | 首页猜你喜欢 |
| 库存预警 | Flink | 低于阈值告警 |
| 实时用户画像 | Spark Streaming | 运营看板 |
| 异常流量检测 | Flink + 规则引擎 | 风控 |

### 6.3 推荐系统

| 推荐类型 | 算法 | 展示位置 |
|---------|------|---------|
| 热门推荐 | Spark TopN 统计 | 首页 Banner |
| 猜你喜欢 | 协同过滤 | 首页猜你喜欢 |
| 相似推荐 | 基于物品协同过滤 | 商品详情页 |

---

## 七、视觉设计系统

### 7.1 配色方案（深空蓝主题）

| 变量 | 色值 | 用途 |
|------|------|------|
| `--bg-body` | `#0B1120` | 页面背景（深蓝黑） |
| `--bg-surface` | `#0F172A` | 卡片/面板背景 |
| `--bg-card` | `rgba(15,23,42,0.8)` | 半透明卡片（毛玻璃） |
| `--color-primary` | `#00D4FF` | 主色调（电光青） |
| `--color-accent` | `#7C3AED` | 辅助色（紫罗兰） |
| `--color-orange` | `#F59E0B` | 促销/秒杀强调色 |
| `--color-green` | `#10B981` | 成功状态 |
| `--color-red` | `#EF4444` | 错误/警告状态 |
| `--text-primary` | `#F1F5F9` | 主要文字 |
| `--text-secondary` | `rgba(241,245,249,0.65)` | 次要文字 |
| `--text-tertiary` | `rgba(241,245,249,0.4)` | 辅助文字 |

### 7.2 设计特色

- **深蓝科技感**：深蓝底 + 青色发光 + 紫色辅助，营造数据/科技氛围
- **毛玻璃效果**：卡片使用 `backdrop-filter: blur()` 实现半透明磨砂质感
- **发光边框**：`box-shadow` + 渐变色实现霓虹边框效果
- **等宽数字**：秒杀倒计时、统计数据使用 `JetBrains Mono` 等宽字体
- **粒子动画**：Banner 区域 Canvas 数据流 + 粒子网络，呼应"大数据"主题
- **统一圆角系统**：`8px / 12px / 16px / 20px / 24px` 五级圆角规范

---

## 八、数据库设计

### 8.1 核心数据表

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `t_user` | 用户信息 | id, username, password, phone, email, create_time |
| `t_product` | 商品信息 | id, name, description, price, stock, category, image_url |
| `t_order` | 订单信息 | id, user_id, total_amount, status, create_time |
| `t_order_item` | 订单明细 | id, order_id, product_id, quantity, price |
| `t_cart` | 购物车 | id, user_id, product_id, quantity |
| `t_flash_sale` | 秒杀活动 | id, product_id, original_price, sale_price, start_time, end_time |
| `t_product_spec` | 规格参数 | id, product_id, spec_name, spec_value |
| `t_product_review` | 商品评价 | id, product_id, user_id, rating, content, create_time |
| `t_user_behavior_log` | 行为日志 | id, user_id, behavior_type, target_id, timestamp |

---

## 九、项目亮点

### 9.1 技术亮点
- **Vue 3 Composition API** 全家桶实践（Pinia + Vue Router + 自定义 Hooks）
- **前端行为追踪 SDK**（Tracker），无侵入式采集用户数据
- **Pinia 跨组件状态共享**，解决 `<router-view>` 无法传递 props 的架构问题
- **Canvas 粒子动画**，纯原生实现，无第三方依赖
- **Spring Boot + Kafka** 异步解耦，行为数据写入不阻塞主业务流程
- **HBase + MySQL** 分层存储，实时数据与聚合数据各得其所

### 9.2 业务亮点
- **数据驱动**：所有分析结果直接反馈到推荐系统、运营策略
- **完整购物流程**：浏览→搜索→详情→加购→下单→支付全链路
- **大数据架构**：Hadoop + Spark + Kafka + HBase 完整闭环
- **深空蓝主题**：差异化视觉设计，契合科技数码电商定位

---

## 十、今日迭代成果（2026-05-12）

| 模块 | 改动内容 | 状态 |
|------|---------|------|
| **全局配色** | 从紫色 `#6366f1` 改为深空蓝主题：深蓝底 `#0B1120` + 青色 `#00D4FF` + 紫色 `#7C3AED` | ✅ 已完成 |
| **Banner 动画** | 新增 Canvas 数据流粒子动画（字符雨 + 粒子网络） | ✅ 已完成 |
| **首页** | 倒计时改青色等宽字体、推荐标签改青色、修复 `recommend-tip` 旧紫色 | ✅ 已完成 |
| **登录/注册** | 输入框聚焦环改青色 | ✅ 已完成 |
| **商品详情** | 数量按钮、加购按钮、进度条 hover 全部改青色 | ✅ 已完成 |
| **购物车** | 图片、数量按钮、推荐卡片 hover 全部改青色 | ✅ 已完成 |
| **订单页** | 订单卡片、步骤条、操作按钮 hover 改青色 | ✅ 已完成 |
| **个人中心** | 背景适配深蓝、标题加渐变文字效果 | ✅ 已完成 |
| **秒杀数据** | 修复数据库时间空档，将秒杀结束时间延长至 23:59 | ✅ 已完成 |
| **搜索状态 Bug** | 修复返回主页搜索状态未清除的 Bug | ✅ 已完成 |
| **分类导航** | 移除 Layout.vue 中的分类导航栏及所有相关代码 | ✅ 已完成 |
| **构建部署** | 完善 Vite 构建 → 后端静态资源 → Maven 重启全流程 | ✅ 已完成 |

---

## 十一、待扩展功能

- [ ] 支付系统集成（支付宝/微信支付）
- [ ] 优惠券系统
- [ ] 会员积分体系
- [ ] 后台管理系统
- [ ] 实时数据大屏（WebSocket 推送）
- [ ] 骨架屏加载动效
- [ ] 页面切换过渡动画
- [ ] 暗色/亮色模式切换
- [ ] 移动端适配优化
- [ ] 智能客服

---

## 十二、快速启动

```bash
# 1. 启动 MySQL（确保端口 3306）
# 2. 启动 Kafka（Linux 集群，确保端口 9092）

# 3. 启动后端
cd e:\shishikan\bigdata-ecommerce\backend
mvn spring-boot:run

# 4. 浏览器访问
http://localhost:8080
```

---

*本报告由 AI 辅助生成，包含项目完整技术架构、功能模块、设计系统及迭代记录。*
